import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
  root: {
    position: 'relative',
    width:'180px',
    height: '70px',
    top: '45px',
    left: '230px',
    background: '#196D3A',
    borderRadius: '100px',
  },
});

export default useStyles;
